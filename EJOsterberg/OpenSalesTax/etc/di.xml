<?xml version="1.0"?>
<!-- SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later -->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:ObjectManager/etc/config.xsd">

    <!--
        Per ADR-001: plugin on Magento\Tax\Model\Calculation::getRate (pattern B).
        afterGetRate reads from the request-scoped QuoteTaxRegistry that the
        totals plugin pre-warms with the OST engine response.
    -->
    <type name="Magento\Tax\Model\Calculation">
        <plugin name="ejosterberg_opensalestax_calculation"
                type="EJOsterberg\OpenSalesTax\Plugin\CalculationPlugin"
                sortOrder="10"
                disabled="false"/>
    </type>

    <!--
        Totals collector plugin: pre-warms the registry in beforeCollect,
        writes the per-jurisdiction breakdown in afterCollect.

        IMPORTANT â€” the target class is the Tax module's totals collector
        (Magento\Tax\Model\Sales\Total\Quote\Tax, registered in
        vendor/magento/module-tax/etc/sales.xml as
        <item name="tax" instance="..." sort_order="450">), NOT the
        Quote module's Magento\Quote\Model\Quote\Address\Total\Tax
        which doesn't exist. Magento's DI compiler silently no-ops
        plugins on non-existent target classes â€” setup:di:compile
        exits clean either way â€” so this bug was invisible at compile
        time. Verified 2026-05-15 against Magento 2.4.7-p3 on VM 914
        (Bug C). Latent since v0.1.0 (b3c8894) until the live demo
        bootstrap surfaced it. Regression test:
        Test/Unit/Etc/DiXmlTargetClassTest.php.
    -->
    <type name="Magento\Tax\Model\Sales\Total\Quote\Tax">
        <plugin name="ejosterberg_opensalestax_quote_totals_tax"
                type="EJOsterberg\OpenSalesTax\Plugin\QuoteTotalsTaxPlugin"
                sortOrder="10"
                disabled="false"/>
    </type>

</config>
